# Keep cross-platform source text normalized while forcing native shell scripts
# and POSIX service/config files to use LF even when checked out from Windows.
* text=auto
*.sh text eol=lf
*.service text eol=lf
*.plist text eol=lf
